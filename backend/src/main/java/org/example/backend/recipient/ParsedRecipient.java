package org.example.backend.recipient;

/**
 * 검증을 통과한 수신자 하나와, 그 수신자가 파일에서 몇 번째 행이었는지를 묶는다.
 *
 * ── 왜 이 클래스가 필요한가 ──
 * 파서가 List<Recipient> 만 넘기면 행 번호가 사라진다.
 * 그런데 서비스 계층은 이 중에서 "이미 DB에 등록된 번호"를 걸러내야 하고,
 * 걸러낸 행에 대해 "38행은 이미 등록된 번호입니다"라고 알려줘야 한다.
 * 행 번호가 없으면 "3건이 이미 등록되어 있었습니다"까지만 말할 수 있고,
 * 담당자는 40행 중 어디인지 눈으로 찾아야 한다.
 *
 * ── 왜 Recipient 엔티티에 line 필드를 넣지 않았는가 ──
 * 행 번호는 '업로드하던 그 순간의 파일 안 위치'일 뿐, 수신자 자체의 속성이 아니다.
 * DB에 저장할 값도 아니다. 엔티티에 넣으면 쓰이지 않는 컬럼이 하나 생기고,
 * 나중에 이 수신자를 다시 조회했을 때 line 이 무슨 뜻인지 아무도 모른다.
 * 그래서 파싱 과정에서만 살아 있는 별도 그릇으로 분리했다.
 */
public record ParsedRecipient(int line, Recipient recipient) {
}